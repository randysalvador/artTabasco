import 'package:get/get.dart';

class SplashController extends GetxController {
  @override
  void onReady() {
    super.onReady();

    //  _init();
  }

  /*  Future _init() async {
    await Future.delayed(Duration(seconds: 2), () {});
    return Get.offNamed(Routes.HOME_VIEW.toRoute());
  } */
}
